<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base theme that extends a Material Components theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- You can add more theme customization here if needed -->
    </style>

    <!-- Custom style for outlined buttons -->
    <style name="CustomOutlinedButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeColor">@color/soft_blue</item> <!-- Define this color in colors.xml -->
        <item name="strokeWidth">1dp</item>
        <item name="android:textColor">@color/white</item> <!-- Define this color in colors.xml -->
    </style>

</resources>
